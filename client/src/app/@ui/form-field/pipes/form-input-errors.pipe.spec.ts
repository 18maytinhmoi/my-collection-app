import { FormInputErrorsPipe } from './form-input-errors.pipe';

describe('FormInputErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormInputErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
